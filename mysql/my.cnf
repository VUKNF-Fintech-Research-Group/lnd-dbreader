[mysqld]
binlog_expire_logs_seconds = 7200

# InnoDB keeps row locks in the buffer pool; MySQL's 128M default
# overflowed (Error 1206) on the dbreader upserts. Host has 23G.
innodb_buffer_pool_size = 1G
